'use strict'

const Factura = require('../modelos/factura.modelo')

function agregarFactura(req, res){
     var params = req.body; 
/*     var facturaModel = new Factura();
     if(params.idUsuario && params.idHotel && params.habitacion && params.idServicio && params.total){
        facturaModel.idUsuario = params.idUsuario;
        facturaModel.idHotel = params.idHotel;
        facturaModel.idHabitacion = params.idHabitacion;
        facturaModel.idServicio = params.idServicio;
        facturaModel.total = params.total;
        facturaModel.save((err, facturaGuardada)=>{
            if()
        })
     }*/
}